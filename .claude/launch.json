{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "clauseguard",
      "runtimeExecutable": "/usr/bin/python3",
      "runtimeArgs": ["-c", "import http.server, socketserver; handler = http.server.SimpleHTTPRequestHandler; httpd = socketserver.TCPServer(('', 8080), handler); print('Serving on port 8080'); httpd.serve_forever()"],
      "port": 8080
    }
  ]
}